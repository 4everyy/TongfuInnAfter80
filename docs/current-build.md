# 当前构建状态

更新时间：2026-08-14

## 当前版本

- 技术路线：微信小游戏原生 Canvas、CommonJS、微信本地存储。
- 项目入口：`D:\TongfuInnAfter80\minigame`。
- 当前主玩法：九域商路 288 格大棋盘，包含产业合作、NPC 交往、随机事件、补给、护路战、岔路与日结经营。
- 复用内容：27 张地图全部作为可进入地标，5 名角色与 72 名特色 NPC 已进入棋盘规则。
- 棋盘主控：佟湘玉固定为主角，其他角色按棋盘成就逐步解锁为伙伴。

## v11 大富翁式棋盘

- 9 个商区、每区 32 格，共 288 格，全部从客栈起点可达。
- 27 张现有地图各对应一个地标，进入后保留八向移动、热点、对话和战斗。
- 72 名 NPC 各自对应一处可合作产业，同时参与来客事件。
- 每日按早上、中午、中午、晚上推进四回合，产业在跨日时统一结算。
- 两支 AI 商队会沿路线移动、建立合作和参与往来费用结算。
- 存档升级至 v11；v10 及更早存档无损迁移到对应地图地标。
- 首页保持单入口，点击“开始棋局”后直接进入大棋盘。
- 本轮 70 个小游戏 JavaScript 文件语法检查通过，26 项无外部图像依赖的回归检查通过。
- `844 × 390` 棋盘首帧、资源失败后备、标题到棋盘入口和损坏存档恢复冒烟检查通过。
- 棋盘视觉已升级为手绘商路舆图，骰子演出延长至约 1.6 秒并在点数落定后才开始走格。
- 棋盘背景已升级为青绿山水武侠世界：山门、河谷、石桥、集镇、驿旗、轻视差和矿物色区域晕染均已接入。

详细设计：`docs/design/grand-board-v1.md`。

## v34 场景接入

27 张新场景已逐图完成运行接入：

- 源图按地图世界尺寸裁切并压缩为 JPEG。
- 柜台、楼梯、桌椅、货箱、栏杆等遮挡物拆为带 Alpha 的 PNG 前景。
- 每个遮挡物使用同一 `obstacleId`关联碰撞、前景和 `sortY`。
- 重新校准可行走区域、连续碰撞、44px 以上出口、出生点、热点和 NPC 脚底站位。
- 江南码头返回点已避开香料商 NPC，防止切图后人物叠在一起。
- 新场景分为 4 个原生分包，均低于内部 3.8 MB 目标。

分包体积：

| 分包 | 大小 |
| --- | ---: |
| `scene-core-v34` | 0.80 MB |
| `scene-s1a-v34` | 0.87 MB |
| `scene-s1b-v34` | 1.18 MB |
| `scene-s2-v34` | 0.88 MB |

此前暂存在 `D:\AI` 的 14 个旧背景和旧遮挡图归档也已删除，不再保留重复运行图。

## 运行资源清理

新场景与新版 NPC 接入后，已按最终运行对象执行资源引用审计：

- 删除 141 个无引用旧资源，共约 6.59 MB。
- 删除内容包括旧地图背景、旧光照、旧遮挡、被新版替换的 NPC、旧头像和构建报告。
- 已移除只剩空入口的 `ch78` 分包；第 7、8 章场景由 `scene-s1b-v34`提供。
- 仍在使用的任务道具已迁入 v34 分包，`scene-core-v23`、`scene-s1-v23`、`scene-s2-v23`三个旧分包已删除。
- 当前资源审计结果：340 个运行资源、0 个未引用、0 个缺失。
- 当前小游戏目录约 14.91 MB，主包约 2.95 MB，共 10 个有效分包。

审计与删除记录：

- `D:\TongfuInnAfter80\outputs\runtime-asset-audit.json`
- `D:\TongfuInnAfter80\outputs\runtime-asset-prune-report.json`

## 验证结果

- 33 项自动功能检查全部通过。
- 128 个 JavaScript 文件语法检查通过。
- 27 张地图全部连通、可逆，无孤立出口。
- 204 段对话、NPC、经营物件和剧情热点保持可达。
- 出生点与 NPC 不落入障碍，不与出口返回点重叠。
- 前景图具有真实透明通道，资源缺失时仍可回退，不造成黑屏。
- 前 11 章、第二季分店、运输、战斗、经营、旧存档迁移和启动诊断回归通过。

## v37 NPC 扩充

- 新增 36 名不同脸型、体型、年龄和动作轮廓的特色 NPC，现有特色 NPC 总数增至 72 名。
- 新增 36 个透明角色精灵和 36 个对话头像，覆盖全部 27 张地图。
- 每名新 NPC 均具备首次对话和重复对话，共新增 72 个可交互热点。
- 角色运行素材统一为 `192 × 256`，脚底锚点固定为 `(96,244)`，无方形底色或跨格碎片。
- 新增 `npc-pop-v37` 原生分包，体积约 1.36 MB；加载失败仍由现有 Canvas 后备流程处理。
- 新 NPC 采用场景边缘错层站位，不阻挡出口、出生点和主要行走通道；江南码头与河渠关卡热点均可到达。
- 生成源图位于 `D:\AI\design-assets\tongfu-npcs\v37\source`，项目目录只保留压缩运行资源。

运行预览：

- `D:\TongfuInnAfter80\outputs\scene-v34-runtime\contact-1.jpg`
- `D:\TongfuInnAfter80\outputs\scene-v34-runtime\contact-2.jpg`
- `D:\TongfuInnAfter80\outputs\scene-v34-runtime\contact-3.jpg`

构建报告：

`D:\TongfuInnAfter80\outputs\scene-v34-build-report.json`

## 下一恢复点

1. 在微信开发者工具中验收 288 格棋盘的缩放、触控、岔路选择与连续移动。
2. 实走地标进入、返回、战斗胜负和中断恢复流程。
3. 将前 11 章改编为棋盘委托链，并增加区域目标和长期胜利条件。
4. 为九区制作独立棋盘地貌、建筑缩略物、骰子和区域解锁演出。
5. 增加本地多人轮流与更完整 AI 商队策略。

## 棋盘图标 v4

- 已接入八类鎏金铜器棋格图标，分别对应地标、产业、侠客、风波、补给、护路、驿马和歇脚。
- 棋格运行图集为 `minigame/assets/art/board/token-atlas-v4.png`，约 0.27 MB；生成源图只保存在 D 盘。
- 主路升级为旧铜包边与赭金嵌线结构，当前格、产业归属和等级继续使用独立状态层，不把状态烘焙进图标。
- 图集加载失败时保留 Canvas 线描后备，棋盘不会因美术资源缺失而黑屏。
- 最新运行预览为 `outputs/grand-board-v5-preview.png`、`outputs/grand-board-v5-dice-preview.png` 与 `outputs/grand-board-v5-jiangnan-preview.png`。

微信开发者工具 CLI 当前提示“服务端口已关闭”。需要在工具的“设置 -> 安全设置”中打开服务端口后，才能由命令行自动触发编译；当前可直接在已打开的工具中手动点击“编译”。

## 存储规则

- 项目源码和优化后的运行资源只放在 `D:\TongfuInnAfter80`。
- 源图、生成图、中间稿和缓存只放在 `D:\AI`。
- 不在 C 盘安装、下载或保存本项目素材。
